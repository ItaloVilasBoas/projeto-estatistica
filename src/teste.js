import Chart from 'chart.js/auto'
import dados from './data.js'


(async function() {
  const data = dados;
  //let media = lista.reduce((total, atual) => total+atual/lista.length, 0);
  //let variancia = lista.reduce((total, atual) => total + Math.pow(media - atual, 2)/lista.length, 0);
  //let desvioPadrao = Math.sqrt(variancia);
  

  
})();